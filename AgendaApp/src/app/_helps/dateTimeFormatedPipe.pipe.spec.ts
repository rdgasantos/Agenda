/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTimeFormatedPipePipe } from './dateTimeFormatedPipe.pipe';

describe('Pipe: DateTimeFormatedPipee', () => {
  it('create an instance', () => {
    let pipe = new DateTimeFormatedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
